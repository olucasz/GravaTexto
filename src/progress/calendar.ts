export function addDays(day:string,amount:number):string {const [y,m,d]=day.split('-').map(Number);return new Date(Date.UTC(y,m-1,d+amount)).toISOString().slice(0,10)}
export function civilDay(at:string,timezone:string,offset=0):string {let parts;try{parts=new Intl.DateTimeFormat('en-CA',{timeZone:timezone,year:'numeric',month:'2-digit',day:'2-digit'}).formatToParts(new Date(at))}catch{parts=new Intl.DateTimeFormat('en-CA',{timeZone:'America/Sao_Paulo',year:'numeric',month:'2-digit',day:'2-digit'}).formatToParts(new Date(at))}const get=(k:string)=>parts.find(p=>p.type===k)!.value;return addDays(`${get('year')}-${get('month')}-${get('day')}`,offset)}
export const formatDay=(day:string)=>new Intl.DateTimeFormat('pt-BR',{day:'numeric',month:'short',timeZone:'UTC'}).format(new Date(day+'T12:00:00Z'));
